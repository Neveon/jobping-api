# Copy to .env for LOCAL dev. Never commit .env.
# NOTE: In production (Railway), FRONTEND_ORIGIN and API_BASE_URL MUST be the
# deployed URLs, not localhost. Double-check these when copying to Railway.

ANTHROPIC_API_KEY=sk-ant-REPLACE_ME
RESEND_API_KEY=re_REPLACE_ME
RESEND_FROM_EMAIL=onboarding@resend.dev
SUPABASE_URL=https://REPLACE_ME.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ_REPLACE_ME

# Local dev defaults — change these in Railway to the deployed URLs:
FRONTEND_ORIGIN=http://localhost:3000
API_BASE_URL=http://localhost:8000

# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_TOKEN=REPLACE_ME
